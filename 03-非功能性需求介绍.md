## 前言

非功能性需求一般指不涉及业务需求的功能，包括：**性能、安全、可用性、合规性**。

在明确了系统做什么的功能性需求之后，架构师面临着一项更具挑战性、也往往更易被忽视的任务：清晰地定义系统做得好不好的问题。

这便是非功能性需求，它关乎系统的质量、约束与运行特性。性能、安全、可用性与合规性这 4 项是需要重点关注的主要非核心功能，它们虽不直接实现业务功能，却从根本上决定了系统能否可靠、高效、合法地承载业务，是架构设计中最关键的约束条件和决策依据。

## 非功能性需求为什么常被忽视

非功能需求常被忽视的原因解析：

**隐性化**

业务方和用户通常默认系统是安全、稳定的。一般情况下出来大的线上事故，才会引起重视。所有就很少主动提出来这些需求。

**技术性强**

性能、安全、可用性，这些都是涉及技术，技术性很强，业务方一般只关心产品的功能什么时候上线，满不满足客户需求。

只有合规性可能涉及到与公司法务相关。

**成本后置**

高性能、高可用性等需求往往意味着更高的前期架构设计与基础设施投入，成本更高，其价值在系统未承受压力或出现故障前不易被感知。

从产品生命周期几个阶段来说，刚开始的 MVP 阶段，确实也不需要特别关注产品使用的高性能、可用性相关需求，只需要快速实现核心功能，上市验证产品功能。

## 非功能性需求说明

### 1、性能

性能需求是关乎用户使用时的体验、系统吞吐量和资源利用率的。可以把它转化为一些可量化的指标，避免实施时的模糊性。

比如说APP用户使用体验要好，这个用户需求在技术层面怎么满足？一般来说就是APP页面显示很快？那这个快的具体标准是什么？

#### 响应时间

 **响应时间** 或 **延迟时间** （Latency / RT），可以用这 2 个不同角度标准来衡量，一个是响应时间有多快，一个响应时间有多慢。

比如用户点击商品浏览详情，商品详情页面多少秒显示完整页面？

商品列表页面加载时间超过多少秒？用户就关掉页面，放弃浏览了？

> 响应时间：请求从发出到收到响应的时间。

> 响应时间的具体指标：平均响应时间(Avg)、P95/P99，用于衡量长尾响应时间，请求在多少毫秒内完成。
>
> P95 和 P99 是统计学中的百分位数（Percentile）概念，在系统性能监控中，它们用来衡量长尾延迟（Tail Latency），即“最慢的那部分请求”有多慢。相比于平均响应时间（Average），P95 和 P99 更能真实反映用户的最差体验。

假设我们将一段时间内（例如 1 分钟）所有的请求响应时间从小到大排序：

P95 (95th Percentile)：表示 95% 的请求响应时间都小于或等于这个数值。

- 换句话说：只有 **5%** 的请求比这个值慢。
- 用户视角：95% 的用户觉得系统很快，但还有 5% 的用户觉得卡。

P99 (99th Percentile)：表示 99% 的请求响应时间都小于或等于这个数值。

- 换句话说：只有 **1%** 的请求比这个值慢。
- 用户视角：99% 的用户体验良好，只有极少数（1%）用户遇到严重卡顿。

#### 吞吐量

吞吐量（Throughput）：单位时间内系统处理的请求数量。

> **QPS** (Queries Per Second): 每秒查询量。每秒钟能处理多少请求
> **TPS** (Transactions Per Second): 事务量。比如：支付接口，需支持每秒3000笔交易（TPS）
> **Bandwidth**: 网络带宽吞吐量

#### 并发数

并发数（Concurrency）：同一时刻系统能够处理的请求数。

#### 资源利用率

资源利用率：在满足性能目标下，对资源的有效利用。

CPU 使用率 Usage %。一般建议 < 70% 预留缓冲。

内存 使用率 Usage %，内存使用率。需关注是否频繁触发 GC (Java/Go)。

磁盘 I/O，I/O 请求平均等待时间。过高说明磁盘是瓶颈。读写带宽 (MB/s)。

网络，网卡流入/流出带宽。丢包率。高性能系统应接近 0。

### 2、可用性

可用性：衡量系统能够正常提供服务的时间比例。它直接关系到产品使用连续性和品牌声誉。

服务可接受的停机时间，业务能容忍系统不可用的时间是多久？

#### 可用性服务等级

下面是衡量可用性的3个指标：

- **服务等级指标 SLI (Service Level Indicator) **

- **服务等级目标SLO（Service Level Objective）**
- **服务等级协议 SLA（Service Level Agreement）** 

**服务等级指标 SLI **：我们测什么？衡量系统健康状况的具体量化指标，它是一个原始数据点或计算结果。

比如：HTTP 请求成功率（如：99.9%），请求延迟（如：P99 延迟 < 200ms），系统吞吐量（如：QPS > 1000）。

服务恢复时间（ < 5分钟）。

SLI 必须是可测量可观测的，一般通过监控日志、Metrics 获取。

**服务等级目标SLO**：我们要达到什么标准。它是基于 SLI 设定的内部目标值，是团队内部对稳定性的期望，通常比 SLA 更严格，留有余地，如果达不到 SLO，团队需要启动改进措施。

比如下面的目标等级，服务可用时间：

> 99.9% (3 个 9) = 一年停机 8.76 小时
> 99.99% (4 个 9) = 一年停机 52 分钟
> 99.999% (5 个 9) = 一年停机 5 分钟

**服务等级指标 SLA **：我们向客户承诺什么？做不到怎么赔？包含惩罚的条款。比如 SLA 的阈值通常低于 SLO（例如 SLO 定 99.9%，SLA 承诺 99.5%），以确保即使偶尔未达内部高标准，也不会触发赔偿。

怎么区分这 3 个指标，来一个开车速度罚款比喻：

> SLI：你开车时的实时车速表读数。
> SLO：你心里设定的目标：“我要保持在限速以内，最好不超过 110km/h"（为了安全留余地）。
> SLA：交通法规：“限速 120km/h，超过就要罚款扣分”。

| 维度         | **SLI (指标)**         | **SLO (目标)**              | **SLA (协议)**                   |
| :----------- | :--------------------- | :-------------------------- | :------------------------------- |
| **关注点**   | **测量** (Measurement) | **目标** (Target)           | **承诺** (Commitment)            |
| **受众**     | 工程师、SRE            | 研发团队、产品经理          | 客户、法务、销售                 |
| **性质**     | 原始数据/计算公式      | 内部管理红线                | 商业合同/赔偿依据                |
| **严格程度** | N/A                    | 最严格 (预留缓冲)           | 较宽松 (底线)                    |
| **后果**     | 无直接后果             | 触发内部改进措施 (冻结发布) | 触发赔偿(罚款/退钱)              |
| **示例**     | "当前成功率 99.8%"     | "目标成功率 ≥ 99.9%"        | "承诺成功率 ≥ 99.5%，否则赔 10%" |

#### 可用性技术措施

**降级与容错**：在一整条链路服务中，发生一些故障或请求并发过大，可以让部分边缘业务降级，以保证重要业务可用性。

**架构设计**：主从架构、多副本数据架构、多地域部署，故障转移机制、异地容灾，监控告警机制等保证整个系统可用。

### 3、安全

安全需求是指保护系统资产（数据、功能）的机密性、完整性和可用性，避免遭到恶意或意外的破坏。

- 敏感性数据：用户的个人信息？包括身份证号、银行卡号等敏感信息。怎么存储，怎么加密，怎么传输，保证数据安全。
- 威胁攻击：SQL注入、跨站脚本、未授权访问、数据泄露、拒绝服务攻击、API访问安全等。
- 身份认证与授权：如何确认用户身份并控制其访问？

等一些安全问题也需要考虑

### 4、合规性

合规性要求系统必须遵守的法律、法规、行业标准及内部政策。这是刚性约束，通常没有商量余地。

**数据保护与隐私**，关键法规：中国的《个人信息保护法》、欧盟的GDPR等。

**行业特定法规**，不同行业有特殊要求。

比如金融行业，需符合金融监管机构的审计、数据保存期限（如交易记录保存5年）、反洗钱规定。

**内部政策**，公司内部的技术标准、数据管理规范等。